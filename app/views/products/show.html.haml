%h2
  Product #{@product.title} with id  #{@product.id}
- @product.attributes.each do |attribute|
  %li
    = attribute.first
    \- #{attribute.last}


MEtafields
- @product.metafields.each do |attribute|
  %li
    = attribute.value
= link_to "Edit", edit_product_path(@product.id )
